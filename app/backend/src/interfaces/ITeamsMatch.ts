/* import Matches from '../database/models/MatcheModel';
import Teams from '../database/models/TeamModel';

export default interface TeamsMatch extends Teams {
  homeMatches?: Matches[];
  awayMatches?: Matches[];
} */
